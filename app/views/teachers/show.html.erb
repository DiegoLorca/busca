<p class="field"><%= @teacher.user.title %></p>
<div class="field">
	Publicado en <%= @teacher.published_on.strftime("%B %e %Y") %>
</div>
<div class="field">
	<%= simple_format @teacher.content %>
</div>
<div class="field">
	<%= link_to "Editar", edit_teacher_path(@teacher) %>  |
  <%= link_to "Volver a Tu Calendario", teachers_path %>
</div>